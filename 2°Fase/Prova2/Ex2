2. Considere que em um time há diversos jogadores. Armazene a lista de jogadores de
um dado time em um ArrayList. Crie um programa Java que:
a) Busque cada jogador individualmente e mostre os seus dados (utilize um foreach);
b) Para um dado time, apresente o total de jogadores;
c) Mostre somente os atacantes para um dado time;
d) Para um dado time, busque e apresente os dados do quarto jogador.

--------------------------------------------------------------------------------------
MAIN 

import java.util.ArrayList;
import javax.swing.JOptionPane;

public class Main {
    public static void main(String[] args) {
        String nTimesTxt = JOptionPane.showInputDialog("Total times:");
        int nTimes = Integer.parseInt(nTimesTxt);
        
        ArrayList<Time> listaTime = new ArrayList();
        
        for (int j = 1; j <= nTimes; j++) {
            Time time = new Time();
            time.setNome(JOptionPane.showInputDialog("Nome time"));

            String nTxt = JOptionPane.showInputDialog("Total jogadores:");
            int n = Integer.parseInt(nTxt);

            ArrayList<Jogador> listaJogadores = new ArrayList();

            for (int i = 1; i <= n; i++) {
                Jogador jogador = new Jogador();
                jogador.setNome(JOptionPane.showInputDialog("Nome"));
                jogador.setPosicao(JOptionPane.showInputDialog("Posição"));

                listaJogadores.add(jogador);
            }
            time.setListaJogadores(listaJogadores);

            //a)        
            String msg = "";
            for (Jogador jogador : listaJogadores) {
                msg = msg + jogador.toString() + " - ";
            }
            JOptionPane.showMessageDialog(null, msg);

            listaTime.add(time);
        }

        // b)
        String nomeTime = JOptionPane.showInputDialog("Nome time");
        
        for (Time time : listaTime) {
            if (time.getNome().equals(nomeTime)) {
                JOptionPane.showMessageDialog(null, time.getListaJogadores().size());
            }            
        }
        
        // c)
        nomeTime = JOptionPane.showInputDialog("Nome time");
        for (Time time : listaTime) {
            if (time.getNome().equals(nomeTime)) {                
                for (Jogador jogador : time.getListaJogadores()) {
                    if (jogador.getPosicao().equals("Atacante")) {
                        JOptionPane.showMessageDialog(null, jogador.toString());
                    }
                }                
            }
        }
        
        // d)
        nomeTime = JOptionPane.showInputDialog("Nome time");
        for (Time time : listaTime) {
            if (time.getNome().equals(nomeTime)) {
                JOptionPane.showMessageDialog(null, time.getListaJogadores().get(3).toString());
            }
        }
    }
}

-------------------------------------------------------------------------------------------------------------------------------

Class Time

import java.util.ArrayList;

public class Time {
    private String nome;
    private ArrayList<Jogador> listaJogadores;
    
    public Time() {}

    public String getNome() {
        return nome;
    }

    public void setNome(String nome) {
        this.nome = nome;
    }

    public ArrayList<Jogador> getListaJogadores() {
        return listaJogadores;
    }

    public void setListaJogadores(ArrayList<Jogador> listaJogadores) {
        this.listaJogadores = listaJogadores;
    }
    
    public String itemA() {
        
        String msg = "";
        for (Jogador jogador : listaJogadores) {
            msg = msg + jogador.toString() + " - ";
        }
        return msg;
    }

    @Override
    public String toString() {
        return "Time{" + "nome=" + nome 
                + ", listaJogadores=" + listaJogadores + '}';
    }
    
    

}

------------------------------------------------------------------------------------------------

Class Jogador

public class Jogador {
    private String nome;
    private String posicao;
    
    public Jogador() {}

    public String getNome() {
        return nome;
    }

    public void setNome(String nome) {
        this.nome = nome;
    }

    public String getPosicao() {
        return posicao;
    }

    public void setPosicao(String posicao) {
        this.posicao = posicao;
    }

    @Override
    public String toString() {
        return "Jogador{" + "nome=" + nome 
                + ", posicao=" + posicao + '}';
    }
    

}
